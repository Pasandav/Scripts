using UnityEngine;
using System.Collections;

public class ControlResplandor : MonoBehaviour {

	/**
	ParticleSystem resplandor;
	int duracion;
	AudioSource sonido;
	public AudioClip laser;

	// Use this for initialization
	void Start () {
		sonido = this.GetComponent <AudioSource> ();

		resplandor = this.GetComponent<ParticleSystem> ();
		duracion = 1;
		resplandor.emission = enabled;



		resplandor.Play ();
		sonido.PlayOneShot (laser, 1f);
		Destroy (resplandor, duracion);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
	**/
}
